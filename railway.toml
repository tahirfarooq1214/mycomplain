[build]
builder = "nixpacks"

[build.env]
NIXPACKS_INSTALL_CMD = "npm install"
NIXPACKS_BUILD_CMD = "cd apps/api && npx prisma generate && npx prisma db push && npx tsc"

[deploy]
startCommand = "cd apps/api && node dist/index.js"
healthcheckPath = "/api/health"
healthcheckTimeout = 30
